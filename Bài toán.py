n = int (input("nhap so nguyen duong n:))


so_chan = 0
so_le = 0

while n>0:
    digit = n % 10
    if digit % 2 == 0
        so_chan += 1
    else:
        so_le += 1
    n //=10

print("so luong so chan",so_chan)
print("so luong so le",so_le)